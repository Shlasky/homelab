services:
  mc:
    image: itzg/minecraft-server:java21-jdk
    volumes:
    # - ./modpacks:/modpacks:ro
    - ./data:/data
    env_file:
      - ../../.env.base
      - .env
    ports:
      - "25565:25565"
    restart: always
