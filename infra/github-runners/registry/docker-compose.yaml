services:
  runner:
    image: myoung34/github-runner
    restart: always
    env_file:
      - ../../../.env.base
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/tmp/runner
    deploy:
      mode: replicated
      replicas: 1
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.3'
          memory: 128M
